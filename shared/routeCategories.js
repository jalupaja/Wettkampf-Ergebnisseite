// Shared enumeration for route categories
export const RouteCategories = Object.freeze({
  QUALIFICATION: 'qualification',
  BONUS: 'bonus',
  FINALE: 'finale'
});

export const RouteCategoryValues = Object.values(RouteCategories);

export default RouteCategories;
